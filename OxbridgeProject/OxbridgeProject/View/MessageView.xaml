<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OxbridgeProject.View.MessageView">
    <Grid>

        <Grid.RowDefinitions>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        <RowDefinition  Height="*"/>
        </Grid.RowDefinitions>


        <Label Text="MESSAGES FROM ADMIN" Grid.Row="0" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
        <ListView Grid.Row="1" Grid.RowSpan="11" Background="aqua" IsPullToRefreshEnabled="True" ItemsSource="{Binding Messages}">
            <ListView.ItemTemplate>
                <DataTemplate>
                   
                    <TextCell Text="{Binding MsgBody}"/>

                </DataTemplate>
            </ListView.ItemTemplate>
            
            
        </ListView>

    </Grid>
</ContentPage>